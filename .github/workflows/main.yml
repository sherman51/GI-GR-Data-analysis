name: Scheduled URL Ping

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping URL 1
        run: |
          echo "Pinging URL 1..."
          curl -L --retry 3 --retry-delay 10 --max-time 30 https://mzjkxsbu3as3cpy3kslh88.streamlit.app/

      - name: Ping URL 2
        run: |
          echo "Pinging URL 2..."
          curl -L --retry 3 --retry-delay 10 --max-time 30 https://uploadpy-ynrhtmnl2zosu6yrhx3cfu.streamlit.app/
